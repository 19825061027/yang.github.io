#+INCLUDE: "../../prefix.org"

#+TITLE: 串口方式传送OS binary到DSRAM问题分析

通过串口传送OS binary到DSRAM并起OS之后，与板子上的硬件串口交互无响应问题分析
按照ssh中所见，ssh的实现逻辑如下：
1. client起ssh进程，通过X11方式走硬件(net)
2. server端起sshd进程，通过硬件监控访问；
3. 有访问到来时，先进行验证；
4. 验证通过之后，起子进程，而子进程的所有标准io流，通过管道传给父进程；
5. 父进程将管道委托给channel
6. channel通过底层硬件实现交互
